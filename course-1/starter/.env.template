# Optional OpenAI-compatible configuration. Never commit a populated .env file.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://openai.vocareum.com/v1
OPENAI_MODEL=gpt-4

# auto: use OpenAI when configured, with deterministic fallback
# deterministic: remain fully local and make no provider requests
SAR_AGENT_MODE=auto
